將日文歌詞學習網頁的更新推送到 GitHub Pages。

執行：

```bash
cd "D:\Sharon\AI\Life\Jepan"
git add index.html
git commit -m "更新內容"
git push origin master:main
```

推送完成後告知使用者，約 1 分鐘後 https://sharonyen55.github.io/japanese-study/ 自動更新。
